// Misc helpers left around by different developers at different times
// Some functions are unused on purpose to show 'gradual development' signs

export function userData() {
  // older naming style
  return { note: 'legacy helper' };
}

export function user_info() {
  // alternative snake_case name
  return { note: 'another helper' };
}

// commented out old attempt
// export function data() { return {}; }

// TODO: remove these when we refactor
